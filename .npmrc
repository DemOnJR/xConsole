# Exact version pins (no ^ or ~) so dependency updates are deliberate.
save-exact=true

# Keep the cooldown protection effective: require the lockfile to be in sync.
# (CI uses `pnpm install --frozen-lockfile`.)
engine-strict=true
