{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Installer window: invoke app commands + listen to install events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
