name: Installer Release

# Builds the one-click xConsole installer — the "clone + compile on your PC" setup.exe
# that lives in installer/ — and publishes it to a rolling GitHub Release.
#
# Channels:
#   main → tag `installer-latest` (marked --latest; public download /releases/latest)
#   dev  → tag `installer-dev`    (pre-release channel; same full installer, from dev)
#
# It re-runs when the installer source changes on main or dev, or via workflow_dispatch.
# No repo secrets are required: GITHUB_TOKEN + preinstalled rustup + gh.

on:
  push:
    branches: [main, dev]
    paths:
      - "installer/**"
      - ".github/workflows/installer-release.yml"
  workflow_dispatch:
    inputs:
      channel:
        description: "Channel to build (main or dev)"
        required: false
        default: "main"
        type: choice
        options:
          - main
          - dev

permissions:
  contents: write # required to create/update the GitHub Release

# One release per channel at a time (don't cancel main when dev builds).
concurrency:
  group: installer-release-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: Show Rust toolchain
        run: rustup show

      - name: Cache cargo build
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            installer/target
          key: installer-${{ runner.os }}-${{ hashFiles('installer/Cargo.lock') }}
          restore-keys: |
            installer-${{ runner.os }}-

      - name: Build the installer
        working-directory: installer
        run: cargo build --release --locked

      - name: Sign the installer (optional)
        shell: pwsh
        working-directory: installer
        env:
          PFX_B64: ${{ secrets.WINDOWS_SIGN_PFX_BASE64 }}
          PFX_PASSWORD: ${{ secrets.WINDOWS_SIGN_PASSWORD }}
        run: |
          if (-not $env:PFX_B64) {
            Write-Host "No signing certificate configured (secret WINDOWS_SIGN_PFX_BASE64 is empty) — shipping unsigned."
            exit 0
          }
          $exe = "target/release/xConsole-Setup.exe"
          if (-not (Test-Path $exe)) { throw "build did not produce $exe" }
          $pfx = Join-Path $env:RUNNER_TEMP "codesign.pfx"
          [IO.File]::WriteAllBytes($pfx, [Convert]::FromBase64String($env:PFX_B64))
          $signtool = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin" -Recurse -Filter signtool.exe |
                      Where-Object { $_.FullName -match '\\x64\\' } |
                      Sort-Object FullName -Descending | Select-Object -First 1 -ExpandProperty FullName
          if (-not $signtool) { throw "signtool.exe not found on the runner" }
          & $signtool sign /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 /f $pfx /p $env:PFX_PASSWORD $exe
          if ($LASTEXITCODE -ne 0) { throw "signtool failed" }
          Remove-Item $pfx -Force
          Write-Host "Signed $exe"

      - name: Package the download
        id: package
        shell: pwsh
        working-directory: installer
        run: |
          $rel = "target/release"
          $exe = Join-Path $rel "xConsole-Setup.exe"
          if (-not (Test-Path $exe)) { throw "build did not produce $exe" }

          $stage = "release-staging"
          if (Test-Path $stage) { Remove-Item -Recurse -Force $stage }
          New-Item -ItemType Directory -Force -Path $stage | Out-Null
          Copy-Item $exe (Join-Path $stage "xConsole-Setup.exe")

          $dll = Join-Path $rel "WebView2Loader.dll"
          if (Test-Path $dll) {
            Copy-Item $dll (Join-Path $stage "WebView2Loader.dll")
            Write-Host "Bundled WebView2Loader.dll alongside the installer."
          } else {
            Write-Host "WebView2Loader is statically linked — shipping a single exe."
          }

          $zip = "xConsole-Setup-windows-x64.zip"
          Compress-Archive -Path (Join-Path $stage "*") -DestinationPath $zip -Force

          "zip=installer/$zip" >> $env:GITHUB_OUTPUT
          "exe=installer/$rel/xConsole-Setup.exe" >> $env:GITHUB_OUTPUT

      - name: Publish rolling installer release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $branch = "${{ github.ref_name }}"
          if ("${{ github.event_name }}" -eq "workflow_dispatch" -and "${{ inputs.channel }}") {
            $branch = "${{ inputs.channel }}"
          }
          $isMain = $branch -eq "main"
          $tag  = if ($isMain) { "installer-latest" } else { "installer-dev" }
          $title = if ($isMain) { "xConsole Installer (latest)" } else { "xConsole Installer (dev)" }
          $sha  = "${{ github.sha }}".Substring(0, 7)
          $date = Get-Date -Format "yyyy-MM-dd"
          $channelLabel = if ($isMain) { "main (stable)" } else { "dev" }

          $notes = @(
            "**One-click xConsole installer** — always the latest build from the **$channelLabel** branch.",
            "",
            "### Install",
            "1. Download **xConsole-Setup-windows-x64.zip** below.",
            "2. Right-click the zip and choose **Extract All**.",
            "3. Double-click **xConsole-Setup.exe** and press **Install**.",
            "",
            "The installer fetches the build tools and compiles xConsole from the **$branch** branch on your own PC (about 10-20 minutes the first time). No admin rights are needed. Running it again later updates xConsole in place on the same channel.",
            "",
            "In the app: **Settings → General → Release channel** switches between Stable (main) and Dev. Updates check that channel and rebuild from it.",
            "",
            "### If your antivirus warns about it",
            "Because xConsole is compiled fresh on your PC, the resulting exe is unsigned and unique to your machine, so it has no reputation yet - some antivirus (e.g. Kaspersky) or SmartScreen may warn or quarantine it. This is a false positive: xConsole is open source and scans statically clean on VirusTotal. To allow it - SmartScreen: More info -> Run anyway; Kaspersky: add the folder %LOCALAPPDATA%\xConsole to Exclusions / trusted apps, and optionally report it at https://opentip.kaspersky.com/. Full details: https://github.com/DemOnJR/xConsole/blob/main/installer/ANTIVIRUS.md",
            "",
            "Built from commit ``$sha`` on $date (branch ``$branch``)."
          ) -join "`n"

          gh release delete $tag --yes --cleanup-tag 2>$null
          $createArgs = @(
            "release", "create", $tag,
            "${{ steps.package.outputs.zip }}",
            "${{ steps.package.outputs.exe }}",
            "--target", "${{ github.sha }}",
            "--title", $title,
            "--notes", $notes
          )
          if ($isMain) {
            $createArgs += "--latest"
          } else {
            $createArgs += "--prerelease"
          }
          gh @createArgs
